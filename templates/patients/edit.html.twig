{% extends 'base.html.twig' %}

{% block title %}Edit Patients
{% endblock %}

{% block body %}
	<div class="container">
		<h1>Edit Patients</h1>

		{{ include('Patients/_form.html.twig', {'button_label': 'Modifier'}) }}

		<a class="btn btn-warning mb-3" href="{{ path('app_Patients_index') }}">Retourner a la liste</a>

		{{ include('Patients/_delete_form.html.twig') }}
	</div>
{% endblock %}
